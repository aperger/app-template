keycloak:
  application-url: http://localhost:8080
spring:
  security:
    oauth2:
      client:
        registration:
          PSSecurity:
            client-id: dummy_client_id
            client-secret: dummy_client_secret
